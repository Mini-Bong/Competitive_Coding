#include <iostream>

using namespace std;
int gcd(int a, int b)
{
    if(a==0)
    {
        return b;
    }
    return gdc(b%a, a);
}
int main()
{
    int a, b;
    cin>>a>>b;
    cout<<"Lcm of tow number is: ";
    
    return 0;
}
